import { createGlobalStyle } from 'styled-components';

export const GlobalIndex = createGlobalStyle`
  body {
    background-image: url('/assets/backgroundPink.png');
    background-size: cover;
  }
`;
